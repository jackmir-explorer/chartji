# Routine: Deep Extract (md 파일 자동 반영)

## 실행 방식
**매일 정오 12:00 자동 실행**
Scout가 6시에 생성한 보고서를 미르가 오전 중 핸드폰으로 확인·체크할 수 있도록
6시간 간격을 두고 실행한다.

**데스크탑에서 수동 실행도 가능:** "deep extract 실행해줘"

## 목적
Scout ⭐ 항목 중 미르가 `[o]` 체크한 논문을
`skills/paper-extract/SKILL.md` 로 정식 knowledge 형식(`.md`)으로 추출하고
**main에 직접 머지**해 즉시 반영한다.

> ⚠ **이 routine은 `knowledge/*.md` 파일 반영까지만 수행**한다.
> 앱 Guide/Hint/Draft에 실제 노출되려면 **Liby 별도 호출**로 `src/knowledge-bundle.js` 컴파일 필요 (수동 — 판단이 많아 자동화 위험).
> 완료 보고에도 이 경고를 반드시 포함한다.

---

## 실행 절차

### Step 1 — 대상 수집

`inbox/scout/` **전체 파일**에서 `[o]` 체크된 항목 수집 (미르가 Scout 파일에서 `[ ]` → `[o]` 로 변경한 항목).

- **오늘자만 처리하지 않는다.** 과거 날짜의 `[o]` 항목도 누락 복구 대상.
- 각 항목의 출처 Scout 파일 날짜를 추적 (요약에 "과거 누락 복구" 섹션으로 표기).
- 대상이 없으면 조용히 종료 (로그 없음 — 정상 동작).

### Step 2 — 논문별 Deep Extract

각 대상 논문에 대해 `skills/paper-extract/SKILL.md` 실행:
1. PubMed/저널 사이트에서 초록 + 결론 수집
2. 임상 핵심 추출
3. knowledge/ 형식 draft 작성
4. 저장 위치 결정 (기존 파일 추가 or 신규 생성)

### Step 2-B — 공부 보고서 생성 (2026-04-24 신설, A층 순수학습용)

Step 2와 **동시에** 각 논문에 대한 공부 보고서를 `inbox/study-notes/` 에 생성한다.

**저장 경로**: `inbox/study-notes/YYYY-MM-DD-[논문제목-slug].md`
- `YYYY-MM-DD` = 실행 당일 KST
- `[논문제목-slug]` = 논문 제목을 kebab-case 영문 슬러그 (또는 짧은 한글). 예: `glp1-adolescent-obesity`, `influenza-rx-model`

**양식**:
```markdown
# {논문 제목}

- **PMID**: {번호}
- **저널 / 연도**: {저널명} {연도}
- **저자**: {제1저자 et al.}
- **출처 Scout**: inbox/scout/YYYY-MM-DD.md

## 초록 요약
{초록 재구성 — 원문 재현 금지, 2~3문단}

## 주요 결과
{핵심 수치·지표·effect size}

## 배경·방법
{왜 이 연구가 필요했는지, 어떻게 설계됐는지 — 교과서적 설명}

## 일차의료 적용 포인트
- {외래 실전 연결 포인트 1}
- {외래 실전 연결 포인트 2}

## 한계·주의
{표본·edge case·외삽 한계}

## 관련 knowledge/ 엔트리
- {Deep Extract가 반영한 knowledge/ md 파일 경로}
```

**격리 원칙** (`rules/file-ownership.md` inbox/study-notes §):
- 본 보고서는 **Liby ingest 대상 아님**
- KNOWLEDGE_BUNDLE·Guide·Hint·Draft 어느 경로에도 흘러가지 않음
- 미르가 **나중에 찬찬히 읽으며 공부하기 위한 자원** 전용

Step 2-B는 Step 2 knowledge/ md 반영 **완료 후** 별도 Write로 생성. 실패해도 Step 2 산출물은 유지 (부분 실패 허용).

### Step 3 — main 직접 머지

> **PR 생성 금지** — 이 routine은 `rules/forbidden.md` "자동 routine 산출물" 예외 적용. `[o]` 체크 자체가 미르의 ingest 승인 신호이므로 PR 게이트 불필요.

작업 내용:
- 추출된 knowledge draft를 해당 `.md` 파일에 반영 (신규 파일이면 Write, 기존이면 해당 섹션에 추가)
- `knowledge/log.md` 업데이트 (신규 항목 추가)
- Scout 파일의 처리된 항목 마커 업데이트 — `[o]` → `[✓]` (과거 파일도 직접 수정)

커밋:
```
feat(deep-extract): YYYY-MM-DD — {논문 제목 축약} 외 N건 (md 반영)

- {논문1 제목 축약} (PMID:xxx) — knowledge/{경로}
- {논문2 제목 축약} (PMID:xxx) — knowledge/{경로}
...

⚠ bundle 반영(앱 실제 노출)은 Liby 별도 호출 필요.
Auto-generated by Deep Extract Routine
```

push: `git push -u origin main` (PR 없이 직접).

### Step 4 — 요약 보고서 생성

`sessions/deep-extract-YYYY-MM-DD.md` 파일을 생성한다.

양식:
```markdown
# Deep Extract — YYYY-MM-DD

## 처리한 논문

| 논문 | knowledge/ 저장 위치 | 공부 보고서 | 태그 | PMID | 출처 Scout |
|---|---|---|---|---|---|
| {제목 축약} | knowledge/{경로} | inbox/study-notes/{파일명} | [CLINICAL] | {번호} | YYYY-MM-DD |

## 핵심 요약

### {논문 제목}
{2~3줄 요약 — 임상 활용 포인트}

---

## ⚠ 다음 단계 필수 — Liby 별도 호출

이 routine은 `knowledge/*.md` 파일 반영까지만 완료됐습니다.
앱 Guide/Hint/Draft에 실제 노출되려면 Liby를 별도로 호출하세요:

```
Liby 불러서 {처리된 md 파일 목록} bundle 반영해줘
```

Liby가 수행할 작업 (자동화 제외 — 판단 필요):
- parents 필드 판단 (child→parent 맥락 확장)
- kind 부여 (disease / drug / topic)
- sections[].sources[] 채움 (TIPS 타입 공식화 포함 — `skills/knowledge-ingest/SKILL.md` 5-B)
- 섹션↔출처 주제 일치 자가검증 (SKILL.md 5-C)
- `src/knowledge-bundle.js` 엔트리 작성
- TRIAGE 감지 확장 (SKILL.md Step 8)

## 과거 누락 복구 (해당 시만)

과거 Scout 파일에서 누락된 `[o]` 항목을 이번 실행으로 복구한 경우 별도 표기:
- {항목} — 출처 `inbox/scout/YYYY-MM-DD.md`
```

### Step 5 — 완료 보고

```
Deep Extract 완료. main 반영됨 (commit {sha}).
⚠ bundle 반영(앱 실제 노출)은 Liby 별도 호출 필요.
요약: sessions/deep-extract-YYYY-MM-DD.md
```

---

## 주의사항

- CLINICAL 항목은 반드시 출처(PMID) 표기
- 기존 파일 내용 삭제 금지 — 추가만 허용
- 한 번 실행당 최대 논문 **10건** (과부하 방지). 10건 초과 시 우선순위 높은 것부터 처리하고 나머지 `[o]` 유지 (다음 trigger에서 재처리). 기존 5건 상한은 2026-04-23 백로그 누적 사례로 상향 (미르 지시).
- 요약 보고서의 "⚠ 다음 단계" 경고는 양식 그대로 유지 — 미르가 헷갈리지 않도록 매번 같은 문구 사용.
- **md 파일과 bundle은 별도 반영 구조**임을 모든 산출물에 명시. 이 구조 자체는 의도된 설계(자동화 위험 관리) — 혼동만 제거하는 것이 이 routine의 책무.

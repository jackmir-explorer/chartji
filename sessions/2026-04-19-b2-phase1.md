# sessions/2026-04-19-b2-phase1.md

## 세션 정보
- 날짜: 2026-04-19
- 작업: B2 스키마 Phase 1 — 섹션 표준 vocabulary + 출처 3-tier 규칙 문서화
- 건드린 파일:
  - `sessions/2026-04-18-b2-schema-design.md` (GitHub main에서 worktree로 sync)
  - `knowledge/section-vocabulary.md` (신설)
  - `knowledge/sourcing-rules.md` (신설)
  - 메모리: `project_b2_draft_template_pending.md` 신설 + `MEMORY.md` 인덱스 추가

---

## Boss 승인서
해당 없음 (구현 워크플로우).

## Designer 설계서

### 배경
- 원 설계서: `sessions/2026-04-18-b2-schema-design.md` (GitHub main에만 있어 worktree에 sync)
- B2 스키마(섹션 딕셔너리 + uiHooks)의 **Phase 1: 설계 확정** 범위만 수행
- Phase 2~5(도구 개정, runtime 지원, 마이그레이션)는 후속 세션

### Coverage 감사 — 실제 79 엔트리 ~30개 md 파일의 `##` 헤더 전수 조사
- 16개 초안 중 `sources` 제거 (primarySources + 섹션별 sources와 3중 중복)
- 고빈도 미매칭 3개 추가:
  - `draft-append` (11/13 disease 파일에 존재)
  - `schedule` (백신 drug 4+ 파일, dosing과 개념 다름)
  - `draft-template` (obesity/vaccination 2개, Working Draft 스켈레톤)
- **최종 18개 표준 섹션** 확정

### uiHooks 개정
- disease 기본값에 `draftAppend: ["draft-append"]` 추가
- drug 기본값 `hint`에 `schedule` 추가 → `["indication","dosing","schedule"]`
- topic 현행 유지
- `draft-template` 라우팅 처리는 **Phase 3 재논의로 보류** → 메모리 저장

### 성공 기준
- `knowledge/section-vocabulary.md` 생성 (18개 dictionary + uiHooks 기본값 + 정규화 규칙 + Auditor drift 감지)
- `knowledge/sourcing-rules.md` 생성 (3-tier 출처 모델 + 태그 규약 + attribution 원칙 + Liby ingest 절차)
- 기존 79 엔트리 md / src/ / agents/ / skills/ 무변경 (Phase 2~3에서)

## Builder 결과

### 생성
1. `knowledge/section-vocabulary.md`
   - 표준 섹션 18개 표 (key · 정의 · 동의어)
   - 자유 섹션 slugify 규칙 + 승격 조건 (3 엔트리 이상)
   - uiHooks 기본값 3종 (disease/drug/topic) + `draft-template` 보류 명시
   - Liby ingest 정규화 5단계 절차
   - Auditor vocabulary drift 감사 기준 및 출력 템플릿

2. `knowledge/sourcing-rules.md`
   - Tier 1/2/3 정의 및 중복 금지 규칙
   - 태그 규약 ([CLINICAL]/[REGULATORY]/[INSIGHTS]/[TIPS]/[META]/[출처 미확인]/[CLINICAL — 조건부])
   - Attribution 원칙 (임의 추정 금지, Researcher 호출 필수)
   - 출처 명명 규칙 (논문/가이드라인/TIPS/규제)
   - Liby ingest 출처 검증 7단계 절차
   - 금지 사항 요약

### 수정·제거
- 없음 (Phase 1은 신설만)

### 메모리
- `project_b2_draft_template_pending.md` 신설 — Phase 3에서 `draft-template` uiHooks 라우팅 재논의 필요
- `MEMORY.md` 인덱스에 1줄 추가

## Reviewer 결과
Designer 설계와 Builder 산출물 일치 확인:
- 18개 key · 정의 · 동의어 표 누락 없음
- `sources` 제거 및 그 근거(primarySources + Tier 2) 명시
- uiHooks 3종 기본값에 draftAppend·schedule 반영
- `draft-template` 보류 명시 및 메모리 링크
- Tier 3계층 정의 + 중복 금지 규칙 명시
- `[출처 미확인]` 태그의 불승격 원칙 명시

파일 책임 경계 위반 없음 — `knowledge/` 내 신설만.

## QA 결과

### 성공 기준 달성
- ✅ `knowledge/section-vocabulary.md` 존재 및 18개 항목 포함
- ✅ `knowledge/sourcing-rules.md` 존재 및 3-tier 모델 정의
- ✅ 기존 79 엔트리·src·agents·skills 무변경

### 미변경 검증 (sanity)
- 이 세션은 Phase 2~3을 건드리지 않았으므로 기존 bundle/runtime은 영향 없음
- 신설 문서는 Liby가 향후 참조할 입력물이며, 현재 런타임에 직접 연결되지 않음

### 검증 가능한 후속 체크 (Phase 2 진입 시)
- 수정된 `agents/librarian.md`와 이 vocabulary 매칭
- 수정된 `skills/paper-extract/SKILL.md` 포맷이 `sections` 딕셔너리 생성
- v2 엔트리 3개(mounjaro/wegovy/urticaria) 변환 후 Chrome 검증

---

## 결과
- 판정: 통과
- 다음 작업:
  - Phase 2 — `agents/librarian.md` ingest skill 개정 + `skills/paper-extract/SKILL.md` B2 포맷 업데이트 (drift 방지 동시 진행 필수)
  - Phase 3 진입 직전 `draft-template` uiHooks 라우팅 재논의 (메모리 참조)

## 회고
- 예상과 달랐던 점:
  - 초안 16개가 고빈도 섹션 대부분을 이미 cover. 빠진 3개는 **현 스키마 필드의 자연 승격**(`draftAppend`, `draftTemplate`) 또는 **kind-specific 패턴**(`schedule`)이었음
  - `sources` 섹션은 1개 엔트리만 사용 → 루트/섹션 레벨 출처와 3중 중복 구조. 제거가 정답
- 다음 세션 반영:
  - Phase 2 진입 시 `skills/paper-extract/SKILL.md` 동시 업데이트 주의 (Deep Extract 루틴이 이 skill 사용 → drift 즉시 발생)
  - `draft-template` 재논의는 Phase 3 runtime 설계 직전에 반드시 미르와 확정

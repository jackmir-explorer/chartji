# Knowledge DB 감사 보고서 — 2026-07-24

> 트리거: 미르 "이후 auditor가 작업해줘" (crp/aaa Liby ingest 직후)
> 범위: `src/knowledge-bundle.js` (967 키) 전수 기계 감사(hard-check) + 신규 crp/aaa 엔트리 + backlog desync
> 방식: 100% 검출 가능한 hard-check(키 중복·dangling wikilinks) 우선. 휴리스틱 항목(교과서중복·앵커링 등)은 이번 범위 밖(별도 심층 감사 시).

---

## 🔴 즉시 조치 권고

### 키 중복 할당 (hard-check) — 4건
`KNOWLEDGE_BUNDLE["X"]`가 서로 **다른 객체로 2회 할당** → 두 번째가 첫 번째를 덮어씀(silent overwrite). 2026-05-07 "20건 데이터 손실 사건"과 동일 패턴. 조치는 미르 결단(통합/분리/폐기).

1. **`GSM`**
   - `line 5561`: `= _menopause_v2`
   - `line 8756`: `= _gsm_v2` ← 승리(덮어씀)
   - 사유: `menopause` 엔트리와 전용 `gsm-genitourinary-menopause` 엔트리가 같은 `GSM` 키를 다툼.
   - 제안: `GSM` 검색은 전용 `_gsm_v2`가 맞을 가능성 높음 → `menopause`에서 `GSM` alias 제거 검토. (미르 확인)

2. **`lateral-epicondylitis`** / **`외측상과염`** (한 쌍)
   - `line 4196/4197`: `= _msk_injection_v2`
   - `line 7529/7531`: `= _occupational_msk_upper_extremity_v2` ← 승리
   - 사유: 주사치료 엔트리 vs 직업성 상지 근골격 엔트리가 외측상과염 키를 다툼.
   - 제안: 어느 엔트리가 외측상과염 primary인지 미르 결정 → 패자에서 alias 제거 또는 두 엔트리 통합.

3. **`post-hospitalization syndrome`**
   - `line 7875`: `= _elderly_nonspecific_symptoms_v2`
   - `line 8830`: `= _transitional_care_v2` ← 승리
   - 사유: 고령 비특이증상 엔트리 vs 노인 전환의료 엔트리가 같은 키를 다툼.
   - 제안: post-hospitalization syndrome 주제 귀속 미르 결정 → 패자 alias 제거.

> 주의: 위 4건 모두 **자동 수정 금지**(Auditor 절대 금지 — bundle 직접 수정 불가). 미르 결단 후 Liby 재컴파일로 반영.

---

## 🟡 검토 권고

### dangling wikilinks — 2건
`[[X]]` 참조가 있으나 X 키·md 파일 미존재 → Obsidian 그래프 미연결.

1. **`[[dementia]]`** (`line 4444`, `elderly-psychotropic-deprescribing` 엔트리 notes)
   - 실제 존재 키: `치매-항우울제-감약`(BPSD 특화, 일반 dementia 아님)
   - 제안: ① 일반 `dementia` 엔트리 ingest, 또는 ② 참조 제거, 또는 ③ `[[치매-항우울제-감약]]`로 교체(단, 문맥은 일반 치매 BPSD 참조라 부정확할 수 있음). 미르 확인.

2. **`[[mrsa]]`** (`line 8923`, `skin-soft-tissue-infection` 엔트리 referral)
   - 실제 존재 키: `skin-soft-tissue-infection`/`SSTI`/`연조직염` (MRSA 전용 엔트리 없음)
   - 제안: 참조 제거 또는 SSTI 자기참조 정리(자기 엔트리 내 MRSA 언급이면 wikilink 불필요). 미르 확인.

### bundle↔md desync (backlog) — 2건
2026-07-03 deep-extract가 md에 실질 추가(각 +62/+53줄)했으나 bundle 미재컴파일 → 학습이 LLM inject/검색에 미도달. (CLAUDE.md "deep extract 절대 원칙" 대상. 엄밀히는 Liby backlog이나 md↔bundle 불일치이므로 감사에 기록.)

1. **`recurrent-uti`** — rUTI AFP2026 추가분(ea82baa) bundle 미반영
2. **`statin-myopathy-management`** — 스타틴 근독성 JGIM2026 추가분(ea82baa) bundle 미반영

- 제안: 다음 Liby ingest 시 두 md 재컴파일 포함(신규 crp/aaa와 함께 SessionStart backlog 4건 중 나머지 2건).

---

## 🟢 이상 없음

- **crp-interpretation** (신규): kind topic, primarySources 2건이 전 섹션 주제 포괄 → sources[] 공백 위반 아님. wikilinks(acute-bronchitis·sinusitis·meningitis·elderly-nonspecific-symptoms·ldh-interpretation) 전부 유효. 키 중복 0.
- **aaa-screening** (신규): kind topic, primarySources 2건(USPSTF·SVS) 포괄. wikilinks(pocus-abdominal·dyslipidemia·smoking-cessation·hypertension) 전부 유효. 키 중복 0.
- **키 중복 hard-check 전체**: 위 4건 외 967키에서 추가 중복 없음.
- **dangling wikilinks 전체**: 위 2건(dementia·mrsa) 외 179개 고유 wikilink 전부 유효.

---

## 다음 단계 (미르 결정 필요)
1. 🔴 키 중복 4건 — 각 키의 primary 엔트리 결정 → Liby가 패자 alias 제거 재컴파일
2. 🟡 dangling 2건 — 제거 vs 엔트리 ingest 결정
3. 🟡 desync 2건 — 다음 Liby ingest에 recurrent-uti·statin 재컴파일 포함

> 미적용 감사 항목(휴리스틱): 교과서급 중복·앵커링·토큰 낭비·bullet↔출처 부조화·5-D 미적용·거대 파일 분할 등은 이번 기계 감사 범위 밖. 필요 시 별도 심층 감사 세션.
